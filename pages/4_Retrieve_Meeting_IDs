from support_file import *

page_configuration("Record Meeting Notes", "✍️")

with st.form("Retrieve_IDs", clear_on_submit=True):
    email_address = st.text_input("**Enter your Email ID**", placeholder="Type here...", key="email_ID", max_chars=50)
    columns = st.columns([12, 1.5], gap="small")
    with columns[0]:
        retrieved = st.form_submit_button("Send meeting IDs to my email ID")
    with columns[1]:
        reseted = st.form_submit_button('Reset')